[package]
name = "vencord-installer-shared"
version = "0.1.0"
edition = "2024"

[dependencies]
vencord-installer-core = { path = "../core", features = ["openasar", "version_check", "generate_asar"] }
tokio = { version = "1.42.0", features = ["full"] }
